FROM alpine:latest
RUN apk add --no-cache postgresql-client git bash
ARG GIT_TOKEN
ARG BRANCH
RUN git clone -b $BRANCH --depth 1 https://github.com/username/db-patches.git
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
ENTRYPOINT ["/entrypoint.sh"]
